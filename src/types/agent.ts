/**
 * Renderer-facing agent types.
 *
 * The canonical definition lives in `electron/contracts/advanced.ts`.
 * Keep this file as a bare re-export so main and renderer stay in sync.
 */
export * from '../../electron/contracts/advanced';
